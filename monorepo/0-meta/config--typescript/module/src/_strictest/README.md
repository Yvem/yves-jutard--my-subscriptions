# "strictest" TypeScript config

A base config extending the "strictest" recommended TypeScript settings.

Goal to be the

- strictest
- most future-oriented
  - most backwards INcompatible
- most conservative in assumptions

then to be extended so that the trade-offs are clear.

**Do not use** those bases directly, see the sibling folder(s).
