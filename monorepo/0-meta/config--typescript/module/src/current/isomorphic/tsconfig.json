{
	"$schema": "https://json.schemastore.org/tsconfig",

	"extends": ["../../_strictest/isomorphic/tsconfig.json"],

	"compilerOptions": {
		"noUnusedLocals": false, // too inconvenient
		"noUnusedParameters": false,
		"isolatedDeclarations": false, // Unfortunately this causes much headache for few benefit (for me) TODO activate it manually on PUBLIC modules
		"types": ["*"] // same
	}
}
